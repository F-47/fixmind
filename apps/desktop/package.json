{
  "name": "@fixmind/desktop",
  "private": true,
  "version": "0.1.7",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "npm run prepare:sidecar && npm run frontend:build && node -e \"const {spawnSync}=require('node:child_process'); const result=spawnSync('cargo',['check','--manifest-path','src-tauri/Cargo.toml'],{stdio:'inherit',env:{...process.env,CARGO_BUILD_JOBS:'1',CARGO_INCREMENTAL:'0'}}); process.exit(result.status ?? 1);\"",
    "package": "npm run prepare:sidecar && tauri build",
    "package:msi": "npm run prepare:sidecar && tauri build --bundles msi",
    "smoke:installed": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-installed-app.ps1",
    "prepare:sidecar": "npm run build:desktop-server -w fixmind && node scripts/prepare-sidecar.mjs",
    "frontend:dev": "vite --host 127.0.0.1 --port 1420",
    "frontend:build": "tsc -p tsconfig.json --noEmit && vite build"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "@tauri-apps/plugin-updater": "^2.11.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.3",
    "@types/node": "^26.6.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "typescript": "^5.8.3",
    "vite": "^8.0.16"
  }
}
